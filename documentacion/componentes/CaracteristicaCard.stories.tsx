import type { Meta } from "@storybook/nextjs-vite";

const meta: Meta = {
  title: "Componentes/CaracteristicaCard",
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component:
          "Componente pendiente de implementacion en src/components/CaracteristicaCard.tsx. Se deja documentado sin ejemplos hasta que exista su API de props.",
      },
    },
  },
};

export default meta;
