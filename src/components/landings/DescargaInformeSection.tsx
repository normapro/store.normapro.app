'use client'

import { FC } from 'react';

type Props = {
    title: string;
    claim: string;
    buttonText: string;
}

const DescargaInformeSection: FC<Props> = ({
    title,
    claim,
    buttonText
}) => {
    return (
        <section>

        </section>
    );
};

export default DescargaInformeSection;