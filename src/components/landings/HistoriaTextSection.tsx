'use client'

import { FC } from 'react';

type Props = {
    title: string;
    claim: string[];
    buttonText?: string;
    image?: string;
}

const HistoriaTextSection: FC<Props> = ({
    title,
    claim,
    buttonText,
    image
}) => {
    return (
        <section>

        </section>
    );
};

export default HistoriaTextSection;